__pycache__/
*.py[cod]
.venv/
models/*.joblib
.env

*.egg-info/

data/
models/distilbert-email*/
models/*-checkpoints/

# mailbox watcher runtime outputs (sample inputs in mail_inbox/ are kept)
mail_quarantine/
review_queue.csv
imap_seen.txt

# Saved copies of scanned live messages (feedback loop working files)
review_messages/

# Poster TIFF is a large derived artefact; regenerate with reports/make_poster.py
reports/poster/*.tiff

# transient image-search scratch
image-search/

# per-folder IMAP state files created by --all-folders
imap_seen_*.txt
review_queue.csv.bak
